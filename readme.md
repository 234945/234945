<!-- TITLE -->
<h2 align="center">234945</h2>

<!-- GO CODE -->
```python
import bitches

class Attributes:
	def __init__(self) -> None:
		bitches.get('bitches')
		
	def contact(self):
	    discord  = "Tenki#2344"
	    tiktok   = "nowayarte"
	    
	    return discord, tiktok

	def life(self):
		langs         = ['Italian', 'Japanese', 'Korean', 'Spanish', 'English']
		nationalities = self.langs.remove('Italy', 'English').append('Korean')
		age           = ?
		
		return langs, nationalities, age
		
	def coding(self):
		langs = {
			'expert':   ['python', 'c++','html'],
			'intermediate': ['go'],
			'learning': ['js']
		}
		specialities  = ['web reverse engineering', 'discord bots'. 'backend']
		environnement = ['vscode', 'pycharm']
		
		return langs, specialities, environnement
		
	def projects(self):
		discord   = ['HQ Gen', 'Raid Toolkit']
		tiktok    = ['view bot', 'Algorithms']
		instagram = ['Gen', 'Botting']
		twitch    = ['Free Gen', 'Everything u can think of']
		website   = ['Full TikTok API', 'Backend']
		
		return discord, tiktok, instagram, twitch, website
